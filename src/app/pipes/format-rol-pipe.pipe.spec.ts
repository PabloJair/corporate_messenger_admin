import { FormatRolPipePipe } from './format-rol-pipe.pipe';

describe('FormatRolPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatRolPipePipe();
    expect(pipe).toBeTruthy();
  });
});
